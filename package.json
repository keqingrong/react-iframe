{
  "name": "react-iframe",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "lerna clean",
    "clean:build": "lerna exec npm run clean",
    "build": "lerna exec npm run build",
    "uglify": "lerna exec npm run uglify"
  },
  "devDependencies": {
    "lerna": "^2.10.1"
  }
}
