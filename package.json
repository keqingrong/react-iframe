{
  "name": "react-iframe",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "clean": "lerna clean",
    "clean:build": "lerna exec npm run clean",
    "bootstrap": "lerna bootstrap",
    "format": "prettier --write '**/*.js'",
    "build": "lerna exec npm run build",
    "uglify": "lerna exec npm run uglify"
  },
  "devDependencies": {
    "lerna": "^2.10.1",
    "prettier": "^1.14.2"
  }
}
